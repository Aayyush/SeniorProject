import * as React from 'react';
import { __ } from '../hocs/i18n';
import { User } from 'ooth-client';
declare type MenuItem = {
    url: string;
    name: string;
    label: string;
};
declare type Props = {
    __: __;
    title: string;
    children: any;
    page: string;
    user: ExtendedUser;
    toggled: boolean;
    setToggled: (toggled: boolean) => void;
    siteName: string;
    menu: MenuItem[];
    userMenu: MenuItem[] | ((user: any) => MenuItem[]);
    loggedMenu?: MenuItem[] | ((user: any) => MenuItem[]);
    footerMessage: string;
    Head: any;
    Link: any;
};
declare type ExtendedUser = User & {
    local: {
        username: string;
        email: string;
    };
    facebook: {
        email: string;
    };
    google: {
        email: string;
    };
};
declare const _default: React.ComponentClass<Pick<Props, "menu" | "title" | "page" | "children" | "toggled" | "setToggled" | "siteName" | "userMenu" | "loggedMenu" | "footerMessage">, any>;
export default _default;
