import * as React from 'react';
declare type Props = {
    onSubmit: (e: React.FormEvent<HTMLFormElement>) => void;
    children: any;
    state?: 'success' | 'error';
    message?: string;
    submitLabel: string;
};
declare const _default: ({ onSubmit, children, state, message, submitLabel }: Props) => JSX.Element;
export default _default;
